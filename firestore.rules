rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DIAGNOSTIC CORE: Allow any auth user
    // We will tighten this to email_verified once flow is confirmed
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}